{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capabilities for the main window: core IPC + opening external links in the system browser.",
  "windows": ["main"],
  "permissions": ["core:default", "opener:default"]
}
